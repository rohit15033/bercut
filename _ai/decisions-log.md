# decisions-log.md
*Append one line per decision. Most recent at the bottom. Read this before every session.*

---

```
2026-03-25 | Single Vite app with routing (not monorepo) | simpler for one dev, one VPS deployment
2026-03-25 | Rumahweb VPS self-hosted — not Railway or Render | client already has VPS
2026-03-25 | PostgreSQL self-hosted on VPS — not Supabase | keeps everything on one server, no cloud DB cost
2026-03-25 | Nginx as reverse proxy, PM2 for Node process | standard self-hosted Node stack
2026-03-25 | Vite build outputs to backend/public — Nginx serves from there | single origin, no CORS issues
2026-03-25 | SSE for real-time — not WebSockets | zero extra infra, native Node/browser, sufficient for queue updates
2026-03-25 | BCA EDC direct integration — Serial/USB ISO 8583 or local TCP | client requirement, offline-capable, no Midtrans
2026-03-25 | Payment model: POSTPAID — pay after service not during booking | client requirement
2026-03-25 | Cashless only — QRIS and card, no cash | client requirement
2026-03-25 | Web Speech API for P1 barber notifications — no Fonnte/Zenziva | free, zero setup, barbers are on premises
2026-03-25 | Web Push PWA for P2 barber notifications | free, works on Android Chrome, no per-message cost
2026-03-25 | Kiosk hardware: touchscreen per branch, not iPad | client requirement
2026-04-04 | Kiosk hardware updated: Android touchscreen (not Windows) — same PWA codebase, no code changes required; lockdown via Android Dedicated Device mode instead of Windows Assigned Access | Follows Xendit Terminal H2H decision; Android is half the cost and has no platform constraints for browser-based PWA
2026-03-25 | No loyalty programme | client decision, not required
2026-03-25 | No expense approval flow — logged immediately | client decision, owner reviews and edits directly
2026-03-25 | Commission: fully configurable per barber, auto-calculated | client requirement
2026-03-25 | Tip: preset Rp 10k/20k/50k + custom + skip, pooled per branch | client decision
2026-03-25 | Inventory: 3 categories — beverages, products, service consumables | client decision
2026-03-25 | EDC integration method: TBC with BCA technical team | open question — confirm before Phase 1 build
2026-03-25 | Claude Code = feature planning + mockups + deploy config + review | workflow decision
2026-03-25 | Antigravity = production execution — frontend/src/apps/ and backend/ | workflow decision
2026-03-25 | Inline styles in mockups — not Tailwind | keeps mockups portable and readable for Antigravity
2026-03-25 | tokens.js as single source of truth for design tokens | prevents drift between mockups and production
2026-03-25 | No backend built until Bercut approves all mockups | avoid rework on schema/routes if UI changes after review
2026-03-25 | Every mockup change request must be checked against schema + API plan before JSX is updated | UI decisions can imply DB columns, status lifecycles, or new endpoints — catch this at mockup stage not build stage
2026-03-25 | No separate Barber App — barber functions moved inside kiosk via PIN-protected panel | Meeting 2 decision; reduces device count, barbers use kiosk for clock-in/out, breaks, start/complete service
2026-03-25 | Kiosk Topbar logo click → Admin or Barber access modal (password-protected) | replaces separate app login; triple-tap StaffPanel remains for payment only
2026-03-25 | Booking terminology is "Reservasi" (not "booking") in all customer-facing UI | client preference; DB column names remain in English
2026-03-25 | Upsell logic defined: Haircut only→suggest beard+treatments; +BlackMask→Mask Cut Pkg; +Beard→Prestige; +Treatment(no beard)→Luxury; +Beard+Treatment→President; if rejected→offer individual services | Meeting 2 final upsell rules
2026-03-25 | Remove "Add On 10K" wash toggle from ServiceSelection; wash now mentioned in service description only | Meeting 2; simplify UI
2026-03-25 | Merge Details screen (name+phone) into Confirm screen — saves one step (4 steps total) | Meeting 2; name required, phone optional
2026-03-25 | Phone number OPTIONAL on confirm screen; WhatsApp checkbox + disclaimer (used for promos, confirmation, points) | Meeting 2
2026-03-25 | TimeSlot: "Now" + "Next Available" are main primary buttons above the time grid | Meeting 2; barber-free = Now enabled; busy/on break = disabled with reason shown
2026-03-25 | Beverages + products (3 each, with image+desc) shown on TimeSlot page after time is selected | Meeting 2; paid at final payment
2026-03-25 | Auto-cancellation: 15 min after booking confirmation; reason added by admin later | Meeting 2; show notice on TimeSlot page
2026-03-25 | Confirm screen shows simple floor plan placeholder with assigned chair highlighted | Meeting 2; chair configurable per branch
2026-03-25 | TOTAL label replaces "ESTIMATED TOTAL" everywhere in booking flow | Meeting 2; barber can still add services, but label is definitive
2026-03-25 | After payment: no "Done" button; auto-redirect to review screen; review has 5-min timeout + skip | Meeting 2
2026-03-25 | DB: add barbers.status ENUM, chairs table, barber_breaks table, booking_extras table, bookings.auto_cancel_at, bookings.cancellation_reason, customers.whatsapp_consent | schema implications of Meeting 2 decisions
2026-03-25 | TV system (queue display, ongoing cuts, wait list, cancellation timer) is out of scope for UI but logic must be captured in SSE event types | Meeting 2
2026-03-25 | Offline mode: kiosk supports reservasi + staff actions without WiFi; sync on reconnect; website booking disabled → redirect to WhatsApp | Meeting 2
2026-03-26 | QueueNumber footer redesigned: "Add Another" + "Done" (onReset) buttons with 72px tap targets; bilingual labels; redundant wait box removed | UI iteration per Bercut feedback
```
2026-03-26 | Font system: Inter for headings/prices/names, DM Sans for body/buttons/labels — Barlow Condensed rejected, too blocky for Bercut brand
2026-03-26 | Tip section redesigned: more aggressive layout with "Popular" badge, removed "No Tip" button for a smaller link, and 100% barber attribution notice.
2026-03-26 | Auto-cancellation disclaimer moved from bottom to the "Selected Time" confirmation box in TimeSlot.jsx for better visibility and a cleaner layout.
2026-03-26 | Package Upsell modal: English-first localization applied to all labels; "Switch to Package" button updated to show savings (e.g. -Rp 10.000) instead of total price.
2026-03-26 | TimeSlot UI: 'Next Available' button hidden if 'Now' is available to prioritize the immediate booking CTA.
2026-03-26 | Auto-cancellation disclaimer in TimeSlot.jsx is now hidden if 'Now' is selected, as immediate chair assignment makes the rule irrelevant.
2026-03-26 | BarberPanel.jsx mockup created — full-screen kiosk panel for logged-in barber (queue, active timer, start/complete, add service, break, clock out); Antigravity prompt added as Section 05B in prompting-guide.md
2026-03-27 | Points reward system added — Meeting 3 decision (reverses "no loyalty programme" from Meeting 1) | Bercut requested customer loyalty via points
2026-03-27 | Points earn rate: Rp 10,000 = 1 point. Earned on cash-paid services + beverages + products. Not earned on points-redeemed services | Meeting 3
2026-03-27 | Points redemption at Confirm screen (Step 4), not PaymentTakeover — customer decides before service starts; mid-cut barber additions are always cash | Meeting 3
2026-03-27 | Points tied to phone number — phone stays optional but kiosk shows nudge "Have points? Enter your WhatsApp number" if phone field is empty | Meeting 3
2026-03-27 | Per-service points toggle in Confirm order summary — customer picks which services to cover with points; packages eligible too | Meeting 3
2026-03-27 | Zero cash total still goes to PaymentTakeover for tip prompt; if no tip either, "Confirm & Complete" skips EDC entirely | Meeting 3
2026-03-27 | DB: add customers.points_balance, booking_services.paid_with_points, bookings.points_redeemed + points_earned, new point_transactions table | schema implications of Meeting 3
2026-03-27 | Kiosk language rule corrected: English primary + Bahasa Indonesia subtitle (was incorrectly documented as Bahasa primary + English subtitle) | Meeting 3 clarification
2026-03-27 | WA consent checkbox removed from Confirm screen; points loyalty nudge/balance moved to that position | Meeting 3
2026-03-27 | Any Available on BarberSelection redesigned as first grid card (same size as barber cards) with 🎲 dice icon | Meeting 3
2026-03-27 | Now and Next Available on TimeSlot redesigned as first two slot cards in the grid (not separate header buttons) | Meeting 3
2026-03-27 | Upsell popup package cards: treatment photos displayed as horizontal strip at top of card; dark gradient overlay; white text floated over | Meeting 3
2026-03-27 | Confirm screen columns flipped: inputs (name/phone/points) on LEFT, order summary on RIGHT | Meeting 3; testing showed users missed input fields when summary was dominant on left
2026-03-27 | Name field on Confirm: auto-focus on mount + pulsing yellow border animation (3 pulses) + instructional placeholder | Meeting 3; user testing showed customers struggled to find/fill name field
2026-03-27 | Floor plan removed from Confirm screen | Meeting 3; chairs table stays in DB for backend logic, not shown in kiosk UI
2026-03-27 | Ear Wax and Ear Candle are mutually exclusive (mutex_group: ear_treatment) — selecting one deselects the other | Meeting 3
2026-03-27 | Beard Trim and Beard Shaving are mutually exclusive (mutex_group: beard_service) — selecting one deselects the other | Meeting 3
2026-03-27 | Package descriptions updated: Ear Wax + Candle → Ear Wax or Candle; Beard Trim or Shave → Beard Trim or Shaving | Meeting 3
2026-03-27 | Package treatment images displayed as horizontal photo strip on package cards in ServiceSelection | Meeting 3; big screen warrants visual treatment representation
2026-03-27 | DB: add image_url TEXT and mutex_group VARCHAR(50) to services table | schema implications of Meeting 3 visual and constraint decisions
2026-03-27 | Kiosk screen is portrait orientation (40-inch); current mockups remain landscape for laptop review, portrait layout revisit deferred | Meeting 3
2026-03-30 | Kiosk upsell popup: chip row split into two states — ✓ muted (already in cart) vs + yellow (new from package); price delta strip added showing +/−/= vs current cart total | UX feedback: users need to see what's new and how much more they'll pay
2026-03-30 | Kiosk UI elements (upsell popups, service ordering, welcome copy, CTA labels, package badge text) must be configurable from admin backoffice — not only hardcoded or on-device | Requirement: kiosk must be modular and remotely manageable per branch without touching code
2026-03-31 | services.category ENUM updated: added `treatment` (was missing — 7 treatment services exist in mockup), renamed `color` → `hair_color` to match mockup cat keys | Schema alignment after kiosk mockup audit
2026-03-31 | booking_services: added bleach_step SMALLINT + bleach_with_color BOOLEAN — needed to store Hair Bleach Configurator choice (1/2/3 steps + optional color add-on) per booking row | Hair Bleach is a variable-price service with step selector in mockup
2026-03-31 | booking_groups table added; bookings.group_id FK added — group payment support seen in StaffPanel (multiple customers pay as one) | Formed at payment time not booking time; points not available for group payments
2026-03-31 | bookings.review_tags TEXT[] added — stores feedback tag strings from ReviewScreen (contextual tags per star rating) alongside existing rating SMALLINT | Post-service rating is now P1 and is part of PaymentTakeover flow, not a separate P3 feature
2026-03-31 | customers.phone changed to VARCHAR(25) E.164 format; phone_country_code VARCHAR(5) added — Confirm screen now has international country code picker (pinned: ID, AU, RU, IN + full list) | Kiosk serves tourists; international phone support required
2026-03-31 | kiosk_settings.tip_presets JSONB added — default [5000, 10000, 20000, 50000, 100000] (mockup updated presets vs original plan's 10k/20k/50k); configurable per branch | Tip amounts need to be remotely adjustable
2026-03-31 | SSE event types and key kiosk API endpoints documented in system-plan.md Section 07 — includes notify-barber, payment-trigger (admin manual), branch-overview, booking-groups | Extracted from mockup API comments during audit
2026-03-31 | Post-service rating + feedback tags promoted from P3 → P1; loyalty/rating removed from Phase 3 roadmap — both fully implemented in kiosk mockup | Phase roadmap updated to reflect actual mockup scope
2026-03-31 | Payroll system added to plan — monthly cycles, salary_plus_commission pay type, individual tip distribution (no pooling), kasbon configurable as current or next month deduction, uang rajin as manual additions with configurable reasons | Meeting 4 decisions
2026-03-31 | Payroll workflow: draft → reviewed → finalized; finalized rows are immutable; only owner role can finalize | Prevents accidental edits after payslips are communicated to barbers
2026-03-31 | Payroll: gross_service_revenue excludes points-redeemed services — barber earns commission only on cash-paid portion | Points redemption reduces the revenue base, consistent with business intent
2026-03-31 | adjustment_reasons table: admin-configurable labels per type (uang_rajin, bonus, deduction, kasbon); can be global or branch-scoped; pre-seeded with common reasons | Reason management in admin Settings sub-section
2026-03-31 | Kasbon pre-logging: admin can log kasbon mid-month before payroll is generated (entry_id = null); system assigns to correct period_month at payroll generation time using target_period_month | Allows kasbon to be recorded immediately when advance is given, not only at payroll time
2026-03-31 | branches.tip_distribution_method added, default individual; branches.pay_period_type added, default monthly | Schema implications of payroll decisions
2026-03-31 | barbers: added pay_type ENUM, base_salary INTEGER, daily_rate INTEGER alongside existing commission_rate | Supports future barber arrangements beyond salary+commission
2026-04-01 | Admin Dashboard mockup created — Overview, BranchDetail, Reports, Expenses, Inventory, Payroll, Settings screens at mockups/admin/; route /mockup/admin added to App.jsx | Completes mockup phase for admin app; light-theme desktop layout, dark sidebar, all decisions-log data applied
2026-04-01 | Admin dashboard uses light theme (bg #FAFAF8, white cards) for main content + dark sidebar (#111110) — consistent with system-plan "White surface, same colour tokens" | Desktop app convention, matches design system
2026-04-04 | Payment provider: Xendit Terminal H2H — confirmed, replaces BCA EDC direct integration entirely | Bercut decision; Xendit is internet-based REST so Android kiosk works; BCA EDC required Windows (Web Serial API) which is double the cost
2026-04-04 | Kiosk hardware: Android touchscreen confirmed — BCA EDC dropped, Xendit Terminal H2H has no platform constraints, Android is half the cost of Windows | Follows from Xendit payment decision
2026-04-04 | Internet dependency accepted for payment fallback — if Xendit Terminal used and internet drops, card payments pause; queue numbers still issue | Bercut explicitly accepted this trade-off
2026-04-04 | Xendit Terminal Indonesia: BRI is only live acquirer; Cashup coming soon — no BRI merchant account needed, funds settle to Xendit Balance T+1 then withdraw to any Indonesian bank | Confirmed via terminal-docs.xendit.co
2026-04-04 | Xendit Terminal integration path: H2H (Host-to-Host) chosen over C2C SDK — H2H is REST API from Node.js backend, keeps PWA architecture intact; C2C SDK requires native Android app | C2C SDK explicitly says "Android/iOS only" — incompatible with PWA kiosk
2026-04-04 | Xendit Terminal supports simulation testing (terminal_id: "SIM001", special test amounts) before physical hardware arrives — Antigravity can build payment flow without waiting for terminal delivery | Confirmed in Xendit H2H quickstart docs
2026-04-04 | Midtrans ruled out — client requirement from Meeting 1; remains excluded regardless of BCA outcome | Original decision stands; Xendit is the approved fallback not Midtrans
2026-04-06 | prompting-guide.md Section 02 (Master System Prompt) updated — corrected 5 stale facts: font (Inter not Barlow Condensed), payment (Xendit H2H not BCA EDC), language order (English primary not Bahasa), tip distribution (individual per barber not pooled), hosting (Rumahweb VPS not Railway/Render) | Audit against decisions-log revealed doc had never been updated after Meeting 3 + 4 changes
2026-04-06 | CLAUDE.md repo structure updated — removed stale barber-app/ mockups folder (never built; barber functions are in kiosk panels since Meeting 2); added all current mockup files | Audit revealed repo structure section was outdated
2026-04-06 | mockups/kiosk/Details.jsx deleted — orphaned file superseded by Confirm.jsx merge (decided 2026-03-25); was never imported in BercutKiosk.jsx | Dead code cleanup
2026-04-06 | tips.barber_id added to schema — tips are individual per barber (Meeting 4), barber_id is required on the tips row for payroll auto-calculation; cannot be reliably derived at query time for group bookings | Schema bug fix
2026-04-06 | system-plan.md sections 07 + 09 updated — hosting (Rumahweb VPS), tips pooling (individual per barber), payment_ref label (Xendit not EDC) | Stale references after Meeting 4 and Xendit payment decision
2026-04-06 | PaymentTakeover: ReceiptScreen added between payment confirmation and ReviewScreen — shows print progress animation, reprint button, WhatsApp send option (if phone provided), auto-advances after 8s | P1 feature was missing; thermal printer is part of Phase 1 spec
2026-04-06 | PaymentTakeover: BCA EDC references replaced with Xendit Terminal throughout | Stale copy from before Xendit decision
2026-04-06 | QueueNumber: escalation countdown UI added — visible progress bar and timer when barber re-announce is pending; "Re-announcing now" state when timer fires; escalation logic was already in state but had no visible UI | P1 feature gap
2026-04-06 | BarberPanel: no-show button added to next booking card — marks booking as no_show, frees slot | P2 feature was missing from panel
2026-04-06 | BarberPanel: late-start indicator added — orange badge "TERLAMBAT Xm" on next booking card + card border turns red after 10+ min; border turns amber at 5+ min | P1 feature (late start tracking) now visible to barber
2026-04-06 | BarberPanel: monthly earnings toggle added — "Hari Ini / Bulan Ini" pill switcher on earnings cards; monthly view shows commission + tips + total bookings for the month with mock data | Improvement: barbers had no way to see monthly earnings from the panel
2026-04-06 | BercutKiosk: offline banner added — bottom strip shows when navigator.onLine is false; confirms queue numbers work but payments are paused | P1 offline resilience requirement was missing from UI
2026-04-06 | BercutKiosk: idle timeout added — after 60s of inactivity on steps 1-5, shows 15s countdown overlay; any tap dismisses; auto-resets to Welcome on expiry | Prevents kiosk being blocked by abandoned mid-flow sessions
2026-04-06 | TimeSlot: out-of-stock state added to beverage and product cards — greyed out, "HABIS" badge, "Stok habis · Out of stock" copy, toggles blocked; outOfStock field added to data.js mock items | Inventory stock levels need to surface on kiosk to prevent orders for unavailable items
2026-04-06 | StaffPanel: group payment formation UI added — "Group Pay" toggle button in header switches to select mode; checkboxes on individual bookings; "Group X Bookings & Pay" CTA when 2+ selected; already-grouped bookings cannot be re-selected; calls POST /api/booking-groups on confirm | Group payment flow previously had display but no formation action
2026-04-06 | KioskConfig.jsx created in mockups/admin — new admin screen with 4 tabs: Welcome Screen (bilingual copy), Upsell Popup (on/off + copy), Tip Presets (add/remove with live preview), Service Order (drag-and-drop); added to BercutAdmin.jsx nav and routing | P2 "Kiosk UI Configuration" feature was planned but buried in Settings; given own screen as per system-plan Section 05
2026-04-08 | Expense categories made admin-configurable — new expense_categories table replaces hardcoded VARCHAR(40) on expenses; expenses.category renamed to category_id (FK → expense_categories); pre-seeded with petty_cash/supplies/utilities/equipment/other; admin can add/rename/deactivate | Hardcoded categories cannot accommodate real-world variation across branches
2026-04-08 | Inventory item types (beverage/product/service_consumable) remain a fixed set — but items within each type are admin-manageable; inventory_items.is_active already in schema | Types are structural; items are data and change regularly
2026-04-08 | Service categories remain a fixed ENUM (haircut/beard/treatment/hair_color/package) — no admin management UI; changing these would alter kiosk flow structure | Not a self-service action
2026-04-08 | Payroll adjustment types fixed — reasons within each type are already configurable (decision 2026-03-31); no change | Types drive calculation logic; labels need flexibility, not the types themselves
2026-04-08 | Dual-access pattern for reference data management: Settings > Catalog tab (central hub: expense categories + inventory item master list) + Inventory page > Manage Items tab + Expenses page > inline Manage Categories panel | Both central setup access and contextual access near each feature
2026-04-08 | New API routes: GET/POST/PATCH /api/expense-categories; GET/POST/PATCH /api/inventory/items — documented in system-plan.md Section 07 API table | Schema additions to support configurable categories and item master list
2026-04-08 | Dual-access pattern corrected: Settings > Catalog tab = governance only (rename/deactivate); creation of expense categories + inventory items happens at point-of-use only (select → ＋ Create modal) | Inline manage panels and Manage Items tab removed; creation where needed, governance centrally
2026-04-08 | Branches screen added to admin dashboard — full CRUD for branches and chair/seat management; chairs table (already in schema) surfaced here; chair assignment enforces one barber per chair per branch at UI level | Branches were previously read-only via Overview; operators need to manage branch config and seats
2026-04-08 | Branch fields editable in admin: name, city, address, online booking slug, timezone, geofence radius, tip method, pay period, onlineBookingEnabled, isActive | Config previously scattered or hardcoded
2026-04-08 | Chair assignment UI: each branch row expands a ChairPanel; chairs shown as grid cards; barber assigned via select; selecting a barber auto-unassigns them from any other chair in same branch | Enforces chair exclusivity at UI level; backend must also enforce at DB level
2026-04-08 | New API routes implied by Branches screen: GET/POST/PATCH /api/branches, GET/POST/PATCH/DELETE /api/branches/:id/chairs | Branch and chair management endpoints needed
2026-04-08 | Service consumables added — new service_consumables junction table (service_id, item_id FK→inventory_items, qty_per_use DECIMAL) links services to the consumable items they use; backend deducts inventory stock when booking_service completes | Consumables (neck paper, blades, wax strips, foil) need to be tracked against the services that use them; manual logging was not viable
2026-04-08 | New API routes: GET/PUT /api/services/:id/consumables | Antigravity reads/writes service_consumables rows via these endpoints
2026-04-10 | User roles: owner / manager / accountant ENUM — role is a label/template only, not a hard access constraint | Configurable permissions per user is more flexible than rigid role-based access
2026-04-10 | All new non-owner users get full access by default — owner chips away rather than building up permissions | Simpler for a small team; most users need most things anyway
2026-04-10 | Owner role is immutable — always full access, permission config ignored, no one can edit owner permissions | Prevents privilege escalation; owner is the protected super-role
2026-04-10 | Permissions are on/off per section — no view vs edit granularity | Sufficient for Bercut's team size; simpler to build and manage
2026-04-10 | User management (create/edit/deactivate accounts) is owner-only — not in the permissions matrix | Prevents self-escalation; who manages users is a structural rule not a configurable one
2026-04-10 | All branches visible to all admin roles — no branch scoping | All roles at Bercut need cross-branch visibility
2026-04-10 | New user_permissions table: user_id + section VARCHAR + is_enabled BOOLEAN — absence of row = access granted | Only store exceptions to the default (full access) rule
2026-04-10 | New audit_log table: user_id, action, entity_type, entity_id, diff JSONB, branch_id (nullable), created_at | Full audit trail replaces need for immutable payroll rows
2026-04-10 | Payroll finalize/immutability removed entirely — payroll rows are always editable | Finalizing too early locks wrong data; audit log provides accountability without hard locking
2026-04-10 | payroll_periods.status ENUM changed: draft → reviewed → communicated (replaces finalized) | "Communicated" is a soft marker only — indicates numbers shared with barbers, no locking enforced
2026-04-10 | payroll_periods: removed finalized_by + finalized_at; added communicated_by + communicated_at | Follows payroll status change
2026-04-10 | Settings screen is fully global — no branch selector at the top level | Simplifies the screen; per-branch config has its own tab
2026-04-10 | Settings tabs: Catalog, Adjustment Reasons, Branch Settings, Users, Audit Log | Users and Audit Log are owner-only tabs; Branch Settings has branch selector inside the tab only
2026-04-10 | Notifications config moved entirely into Settings → Branch Settings tab — no global default, no override pattern | Simpler: one place for per-branch operational settings, no sync between global and per-branch
2026-04-10 | Branch Settings tab contains: late start threshold, speaker on/off, tip presets, online booking toggle, Web Push settings | All per-branch operational config in one place
2026-04-10 | Branches screen remains structural only: name, address, chairs, tip method, pay period, geofence, active/inactive | Branches = what a branch is; Branch Settings tab = how it behaves
2026-04-10 | Audit log lives in Settings → Audit Log tab, global scope (no branch filter) | Owner reviews all activity across all branches in one view
2026-04-10 | GPS geofencing removed entirely — no geofence_lat/lng/radius on branches, no location columns on attendance | Kiosk is physically fixed to the branch; barber presence is confirmed by kiosk login, GPS redundant
2026-04-10 | Face ID verification dropped — was solving proxy clock-in problem that physical kiosk already solves | No separate barber app = no remote clock-in = no proxy risk
2026-04-10 | attendance table simplified: keeps barber_id, branch_id, clock_in_at, clock_out_at only | All location and biometric columns removed
2026-04-10 | adjustment_reasons table dropped entirely — replaced with hardcoded reason list per type + "Custom..." free-text fallback in Payroll UI | Configurable reason templates too complex for how rarely they change; hardcoded list keeps reports consistent without management overhead
2026-04-10 | payroll_adjustments.reason_id FK dropped; reason_text VARCHAR(200) stays — stores selected label or custom text | Follows adjustment_reasons removal
2026-04-10 | Settings Adjustment Reasons tab removed — Settings now has 4 tabs: Catalog, Branch Settings, Users, Audit Log | Follows adjustment_reasons simplification
2026-04-13 | Point expiry policy: inactivity-based — clock resets on every completed booking; global (chain-wide), not per-branch | Loyalty is chain-wide; per-branch expiry too complex
2026-04-13 | points_expiry_months SMALLINT DEFAULT 12 added to global settings (0 = never expire); points_expiry_warning_days SMALLINT DEFAULT 30 | Configurable via Settings > Loyalty tab; applies chain-wide
2026-04-13 | customers.points_last_activity_at TIMESTAMPTZ added — set on every completed booking; backend cron runs nightly, inserts 'expired' point_transaction row and zeros balance when now > points_last_activity_at + expiry interval | No grace period; expiry is immediate once past the threshold
2026-04-13 | point_transactions.type ENUM gains 'expired' — expiry cron inserts one negative transaction row per expired customer; immutable log requirement | Expiry is a transaction event, traceable in history
2026-04-13 | service_branch_prices renamed → branch_services; is_available BOOLEAN DEFAULT true added — enables per-branch service hiding in addition to price overrides | Bercut requirement: some branches don't offer all services (Hair Highlight / Hair Bleach are specialist-only; smaller branches skip some treatments)
2026-04-13 | PUT /api/services/:id/branch-prices renamed → PUT /api/services/:id/branch-config — payload now { branch_id, is_available, price } | Follows branch_services schema change; kiosk GET /api/services?branch_id= already filters by availability server-side
2026-04-13 | Commission is now per-service, not a single flat rate per barber — services.commission_rate added; barbers.commission_rate becomes fallback only (used when a service has no rate set) | Bercut requirement: different services pay different commission rates (e.g. haircut 40%, treatment 35%, hair color 45%)
2026-04-13 | payroll_entries.commission_rate_snapshot removed — no longer a single rate; commission_earned now stores the pre-summed result of Σ(service_rate × price_charged) per booking_service; effective rate shown in payroll UI is blended (commission_earned / gross_revenue) | Follows per-service commission decision
2026-04-13 | barber_service_rates table deferred to P2 — per-barber overrides on specific services not needed yet; all barbers earn the same service rate for now | Can add if a senior barber negotiates different rates per service in future
2026-04-13 | branch_services.commission_rate added — commission % is overridable per branch via the Services > Configure panel; cascade is branch_services.commission_rate > barbers.commission_rate | No global service-level commission_rate field — admin UI does not expose it; per-branch config is the only place to set commission rates
2026-04-13 | services.commission_rate removed from schema and admin UI — commission is configured per-branch only in branch_services; falls back to barbers.commission_rate when not set | Simpler model: no global service rate needed since per-branch config covers all cases
2026-04-14 | expenses.source ENUM('petty_cash', 'owner') added — two fixed options, not configurable; owner name (Raynand) stored as a display label only | Meeting 5; source = funding origin, separate from category
2026-04-14 | expenses.receipt_url changed from optional to NOT NULL — receipt upload is now mandatory with no exceptions | Meeting 5
2026-04-14 | expenses.branch_id now supports all branches + Head Office — Head Office added as a special branch row (is_head_office BOOLEAN DEFAULT false on branches table); selectable in expense form alongside all branches | Meeting 5; head office expenses need a home
2026-04-14 | barber_services table added (barber_id, service_id, is_enabled BOOLEAN DEFAULT true) — tracks which services each barber is capable of performing; scoped globally per barber not per branch | Meeting 5; branch availability (branch_services) and barber capability (barber_services) are separate concerns; kiosk filters barbers where both are true
2026-04-14 | Kiosk barber selection: barbers who cannot perform the selected service (barber_services.is_enabled = false) are hidden from the grid entirely | Meeting 5
2026-04-14 | Barber service capability managed on Barbers page — new "Services" tab on each barber profile; toggles per service | Meeting 5
2026-04-14 | Reports > Barber Performance: barber rows are now clickable — opens individual barber transaction log (date, service, amount, commission, customer name); filterable by date range; exportable to CSV | Meeting 5
2026-04-14 | LiveMonitor.jsx created — new admin screen accessible to all roles; CCTV monitoring team's primary screen; triggered by clicking In Chair stat on Overview; shows all barbers per branch (even idle ones), current customer, services selected, next booking, time started, estimated time, time taken; updates live via SSE | Meeting 5
2026-04-14 | monitoring role added to users ENUM — restricted access: Overview + LiveMonitor only; all other roles can also access LiveMonitor | Meeting 5
2026-04-14 | Barber schedule screen replaced entirely — fixed weekly schedule concept removed; replaced with unified Attendance + Payroll page (two tabs) | Meeting 5
2026-04-14 | Attendance tab: monthly table view; rows = barbers; columns = days of month; tracks present/absent, late minutes, excused/inexcused off, which branch per day (rotation support), surat dokter flag per off day; barber rows clickable for day-by-day detail | Meeting 5
2026-04-14 | off_records table added (barber_id, branch_id, date, type ENUM('excused','inexcused'), has_doctor_note BOOLEAN DEFAULT false, note TEXT, logged_by INTEGER FK→users, created_at); surat dokter can be added at logging time or flagged retroactively; has_doctor_note = true → no deduction regardless of quota | Meeting 5
2026-04-14 | attendance.branch_id already present — sufficient to track which branch a barber worked at per day for rotation | Meeting 5
2026-04-14 | Payroll tab: same monthly data as Attendance tab plus monetary deduction columns; all auto-calculated deduction amounts are individually editable per row in the table (admin adjusts if too harsh — e.g. reduce late deduction due to personal circumstances) | Meeting 5
2026-04-14 | Payroll deduction model: fixed monthly salary + commission earned - (late deduction + inexcused off deduction + over-quota excused off deduction); no kasbon; no manual adjustment categories; accountant handles any further adjustments in Excel | Meeting 5
2026-04-14 | Kasbon removed from payroll entirely | Meeting 5
2026-04-14 | Manual payroll adjustment categories (uang rajin, bonuses, deductions) removed from system — too flexible to standardise; accountant manages in Excel | Meeting 5
2026-04-14 | payroll_settings table added (global): late_deduction_per_minute INTEGER, inexcused_off_flat_deduction INTEGER, overlimit_excused_off_flat_deduction INTEGER, off_quota_per_month SMALLINT DEFAULT 4; configurable in Settings | Meeting 5
2026-04-14 | payroll_entries: deduction columns are editable overrides — late_deduction_override, inexcused_off_deduction_override, overlimit_excused_deduction_override (nullable; when set, override the auto-calculated value) | Meeting 5; supports per-case leniency without removing the automation
2026-04-14 | payroll_periods.status: draft → reviewed → communicated (unchanged from Meeting 4); payroll is always editable, no locking | Unchanged
2026-04-14 | Payroll all-branch view added — branch selector on Payroll page includes "All Branches" option for viewing across branches; CSV export works for all-branch or single-branch selection | Meeting 5
2026-04-14 | Inventory logging merged into Expenses — when logging an expense, a checkbox "This expense received stock" triggers inline inventory receive fields (item, quantity, unit); stock is only receivable via an expense (no free receipt flow); inventory screen becomes monitoring-only (stock levels + movement history, no actions) | Meeting 5
2026-04-14 | expense_stock_items table added (expense_id FK→expenses, item_id FK→inventory_items, quantity_received DECIMAL, unit VARCHAR(20)) — links a stock receipt to its expense | Meeting 5
2026-04-14 | inventory_movements: existing stock-in records will be created by the backend when an expense with has_stock_receipt=true is saved; distribution (stock-out to branches) remains in expense form as a second checkbox | Meeting 5; question deferred — distribution flow TBC (may also require expense association)
2026-04-14 | Barber off-day quota: 4 days/month default, configurable in payroll_settings.off_quota_per_month; once a week rule is not enforced by system (admin manages manually) | Meeting 5
2026-04-15 | Kasbon re-introduced (reverses 2026-04-14 removal) — logged via Expenses page, not Payroll; creates expense record for cash-flow tracking and auto-deducts from barber's payroll at month-end | Post-Meeting 5 decision; logging at point of payment (Expenses) is more natural than waiting until payroll run
2026-04-15 | Expenses screen refactored: three explicit types replace single-form-with-toggle — Regular (operational spend), Inventory (stock purchase with distribution), Kasbon (salary advance tied to barber); type picker at top of form, each type has tailored fields | Cleaner UX than a morphing form with hidden sections; types are structurally distinct
2026-04-15 | expenses.type ENUM('regular', 'inventory', 'kasbon') added to schema; expenses.barber_id FK→barbers (nullable, required for kasbon type only) | Schema implications of three-type expenses decision
2026-04-15 | Kasbon category filter hidden when type filter = kasbon; category filter applies to regular/inventory only (kasbon has no expense category) | UX decision — avoids confusion in the expense list filter bar
2026-04-15 | Payroll adjustments re-introduced (reverses 2026-04-14 removal) — two types: Additions (Uang Rajin, Bonus) and Deductions (Late Arrival, Equipment Damage, Uniform, Absence Without Notice); configurable categories with Custom free-text fallback; Remarks field per adjustment | Post-Meeting 5 decision; accountant-in-Excel approach was too disconnected from the system
2026-04-15 | Kasbon deductions in payroll are auto-imported from Expenses (type=kasbon, barber_id match) — read-only in payroll, locked with 🔒; managed/deleted from Expenses page only | Single source of truth for kasbon; prevents duplicate entry and divergence
2026-04-15 | Adjustment categories: hardcoded base list per type (Additions: Uang Rajin, Bonus, etc; Deductions: Late Arrival, Equipment Damage, etc) + admin can create new categories inline via "＋ Create Category..." in the dropdown — replaces free-text Custom fallback | Created categories are persistent and reusable; better for consistent reporting than a raw free-text field
2026-04-15 | payroll_adjustments table added to schema: id, payroll_entry_id FK, type ENUM('addition','deduction'), category VARCHAR(60), category_label VARCHAR(60), remarks TEXT, amount INTEGER, by INTEGER FK→users, date DATE, is_kasbon BOOLEAN DEFAULT false, expense_id FK→expenses (nullable) | Schema implications of adjustments re-introduction
2026-04-15 | Net pay formula: base_salary + commission - late_ded - inexcused_ded - excused_overlimit_ded + total_additions - total_deductions (total_deductions includes kasbon) | Additions and deductions from payroll_adjustments are part of the same net pay calculation
2026-04-15 | Kasbon deferral UX: two-place control — (1) Expenses form has "Deduct in: This Month / Next Month" toggle at log time; (2) Payroll Adjustments modal has "Defer → / ← Restore" button on kasbon rows to change period after the fact | Operator needs to set default at log time but also correct it from payroll without going back to Expenses
2026-04-15 | payroll_adjustments.deduct_period ENUM('current','next') added — controls whether a kasbon deduction applies to the current payroll period or is deferred to next; default 'current'; also stored on expenses.deduct_period for kasbon-type expenses | Schema implication of kasbon deferral feature
2026-04-15 | Deferred kasbon excluded from net pay calculation for current period — net pay only subtracts kasbon adjustments where deduct_period = 'current'; deferred rows are visible in the adjustments modal but greyed out with "Deferred → [next month]" badge | Prevents over-deduction; deferred amount rolls into next month's payroll_adjustments
2026-04-15 | Payroll adjustments UX: split into two separate modals — ManageAdjModal (opened by row click, shows list of existing adjustments with defer/restore controls) and AddAdjModal (opened by "+ Add" button with e.stopPropagation, shows form only); combined modal was hard to use | Separation of concerns: view/manage vs create; reduces cognitive load per modal
2026-04-15 | Kasbon 🔒 symbol removed — replaced with functional "Defer →" / "← Restore" toggle button on kasbon rows in ManageAdjModal | 🔒 was unclear and read as permanently locked; Defer/Restore button communicates the action directly
2026-04-16 | Purchase Order (PO) system added for multi-payment inventory expenses — advance payment sits unattributed (branch_id NULL) while order is open; when final payment closes the order, LRM attribution is applied retroactively to all payment expense rows via po_attribution JSONB; no data is "changed" (unattributed → attributed is completing not correcting) | Large orders require down payments before stock arrives; cash flow must be recorded at payment time but branch cost attribution can only be determined when goods are received and distributed
2026-04-16 | expenses schema additions: type ENUM('regular','inventory','kasbon'), barber_id FK (kasbon only), deduct_period ENUM('current','next') (kasbon only), po_id FK→purchase_orders, po_payment_type ENUM('advance','final'), po_attribution JSONB | Schema implications of three-type expenses and PO system
2026-04-16 | purchase_orders table added — tracks open/closed PO lifecycle, paid_amount running total, distributions JSONB (populated at close), created_by | New table required to manage multi-payment order state and link payment expense rows together
2026-04-16 | Excel export (xlsx) planned for Expenses, Reports, and Payroll — backend library: exceljs; multi-branch/PO expenses flatten into one row per branch-attributed line; two-sheet format (Summary + Transactions pivot-ready); accounting software integration (Jurnal/Accurate) deferred to later via format= query param | Accountant needs xlsx for handoff; CSV insufficient for multi-branch attribution presentation and accounting software import
2026-04-17 | Payroll split cycles: Commission + Late Fees use 16th prev month → 15th current month; Base Salary + Offs use 1st → last day of same month; Tips period TBD (pending decision) | Different payroll components have misaligned natural cutoffs — a unified period_month cannot correctly represent all three
2026-04-17 | payroll_periods gains 6 explicit date range columns: attendance_from/to, commission_from/to, tips_from/to (nullable) — backend pre-fills defaults at generate time but admin can override via 3 date range pickers in GenerateModal before confirming | Allows custom ranges per component and makes the calculation window explicit rather than derived from period_month
2026-04-17 | Eff. Rate column removed from Payroll barber entries table — per-service commission rates make a single blended % misleading; absolute commission_earned stays | Same reasoning as barber TX modal in Reports: blended rate implies one rate applies to everything, but it doesn't
2026-04-17 | Kasbon modal removed from Payroll page — kasbon is logged in Expenses (type=kasbon) and auto-imported as read-only deduction rows in payroll; Kasbon log tab also removed | Single source of truth for kasbon; prevents double-entry
2026-04-17 | Payroll adj UX: ManageAdjModal (row click → auto-deduction overrides + adj list + defer/restore for kasbon rows) + AddAdjModal (stacks on top, z-index 300 over ManageAdjModal at 200) | Separation of concerns — view/manage vs create; reduces cognitive load vs the previous combined inline expansion
2026-04-17 | Payroll period unified to 16th–15th for ALL components (reverses split-cycle decision above) — Base Salary, Commission, Late Fees, Tips all use the same window; simplifies admin UX and schema | Bercut decision; single cycle is easier to communicate to barbers
2026-04-17 | payroll_periods schema simplified: 6 explicit date-range columns (attendance_from/to, commission_from/to, tips_from/to) collapsed to 2 — period_from DATE (16th) + period_to DATE (15th next month) | Unified cycle makes the 3-range model redundant
2026-04-17 | Payroll period named by START month — "April" period = 16 Apr → 15 May; label shown as "Apr – May" in UI; payout on 30th/31st of start month | Naming by end month (May) was confusing since May is when you pay April's work
2026-04-17 | Kasbon calendar-month assignment: kasbon entered ANY date in April (1st–30th) → April payroll period, regardless of 16th–15th window | Payout is 30th/31st of calendar month; kasbon taken in April is deducted from the April payout; 16th–15th boundary is irrelevant for kasbon timing
2026-04-17 | Kasbon gets its own dedicated column in payroll table — separate from Deductions column; Deductions = late/inexcused/adj deductions only; Kasbon = auto-imported kasbon sum | Gives finance a clear view of salary advances vs performance/attendance deductions
2026-04-17 | Payroll deduction auto-recalculation: editing the count (late minutes, excused times, inexcused times) now recalculates the deduction amount immediately — no separate amount override; count is the single source of truth | Eliminates the two-input confusion where count and amount could diverge
2026-04-17 | Attendance deduction model: two methods per off type — Flat (Rp 150.000/day) and Pro-rata (baseSalary ÷ workingDays × days); both can apply simultaneously for different days within the same period | Some absences warrant a fixed penalty; others are better treated proportionally — admin splits the count between two inputs per row
2026-04-17 | payroll_entries: inexcused_fixed_days + inexcused_prorata_days + excused_fixed_days + excused_prorata_days columns added (all INTEGER DEFAULT 0); nullable overrides; backend defaults are all fixed, zero prorata | Schema implications of split deduction model
2026-04-17 | Late deduction rate: LATE_RATE_PER_MIN = Rp 2.000/min (derived from existing data); Flat off deduction rate: FLAT_OFF_RATE = Rp 150.000/day; both constants defined in payroll settings | Rates are configurable in payroll_settings table
2026-04-17 | Working days formula: period_days × (6/7) rounded — barbers work 6 of 7 days but their day off rotates (not a fixed weekday per barber); admin can override the computed value for public holidays | Single formula, not weekday counting; override stored in UI state only (not DB — it's a per-period review adjustment)
2026-04-17 | Overtime commission added — services completed at/after a configurable threshold time earn an additional bonus % on top of the barber's standard commission rate; excluded service list is configurable; Just a Haircut excluded by default | Late-hour services carry more demand; incentivises barbers to take late bookings
2026-04-17 | overtime_commission_config table added (or global settings row): enabled BOOLEAN, threshold TIME, bonus_pct DECIMAL(4,2), excluded_service_ids INTEGER[] | Schema for OT commission settings; managed in Settings > Payroll tab
2026-04-17 | payroll_entries.commission_regular + commission_ot added — commission_earned split into two components; commission_ot = bonus portion only (OT_revenue × bonus_pct); base commission on OT services is in commission_regular; commission_regular + commission_ot = commission_earned | Payroll table shows the breakdown so accountant can audit OT commission separately
2026-04-17 | Settings > Payroll tab added — configures overtime commission: enable/disable toggle, threshold time, bonus %, service exclusion checklist grouped by category | Config lives in Settings not hardcoded
2026-04-17 | Attendance and Payroll separated into two distinct admin pages — currently one page (two tabs); decision: split into separate nav items | Different audiences (manager checks attendance daily; accountant runs payroll monthly), different cadences, both have grown complex enough to stand alone; Payroll page will have a "View Attendance" shortcut for cross-reference
2026-04-19 | Branches added to sidebar nav between Barbers and Services | Structural + operational branch config warrants own nav item; Settings no longer has any branch config
2026-04-19 | Branch Settings tab removed from Settings entirely — operational config (speaker, push, late threshold, ack grace, tip presets, online booking toggle) moved into Branches screen edit modal under an Operations tab | Consolidates all branch management in one place; Settings = global only
2026-04-19 | BranchModal now has two tabs: Details (name, city, address, timezone, payroll, tip method, active) and Operations (online booking, speaker, web push, late threshold, ack grace, tip presets) | Single screen for everything about a branch
2026-04-19 | Chair system: permanent assignment via chairs.barber_id; temporary or indefinite overrides via new chair_overrides table (chair_id, barber_id, date_from DATE, date_to DATE nullable, reason TEXT, created_by FK→users) | Chair-centric model; simpler than barber_rotations; covers rotations, absences, freelancers, transfers
2026-04-19 | chair_overrides.date_to = NULL means indefinite — original barber resumes automatically only when override is removed or a new permanent assignment is made | Covers fired/transferred barbers without forcing an end date
2026-04-19 | Active barber at a chair on date D = chair_overrides if active override exists today, else chairs.barber_id; override takes priority | Kiosk query: JOIN chairs + chair_overrides with date filter; COALESCE(override.barber_id, chair.barber_id)
2026-04-19 | Barber excluded from home branch kiosk when they are barber_id in an active chair_overrides row at a different branch — prevents double-booking across branches | Home chair still has their permanent assignment; kiosk filter strips them while they're covering elsewhere
2026-04-19 | barbers.branch_id nullable — freelancers have branch_id = NULL; pay_type = daily_rate; no commission; no base salary | Freelancers have no fixed home branch; can be assigned chairs at any branch via chair_overrides
2026-04-19 | Freelance payroll: appears in payroll of each branch they worked at; net_pay = attendance_days × daily_rate; zero deductions; no commission columns | Trivially simple; payroll_entries created per branch they clocked in at
2026-04-19 | Kiosk hardware corrected: Windows touchscreen (not Android) — reverses 2026-04-04 Android decision; Xendit H2H works on both; Windows Assigned Access replaces Android Dedicated Device mode for browser lockdown | Client clarification
2026-04-19 | Kiosk device authentication: permanent device token stored in localStorage — sent as X-Kiosk-Token header on every API call; no token = Device Not Registered screen; URL alone is not sufficient (anyone with URL could make bookings) | Security requirement; URL-only access has no auth boundary
2026-04-19 | Kiosk device tokens are permanent until manually revoked — fixed device, not a user session; admin revokes from Branches → Kiosk Devices tab if device is lost or replaced | Time-expiring tokens would break the kiosk overnight requiring manual re-auth every day
2026-04-19 | Kiosk Devices tab added to Branch modal as third tab (after Details + Operations) — lists registered devices, generate token, revoke per device; token shown once at generation time only | All branch management in one place; token management belongs next to branch config
2026-04-19 | kiosk_tokens table added: id UUID PK, branch_id FK, token VARCHAR(64) UNIQUE (stored hashed), device_name VARCHAR(100), is_active BOOLEAN DEFAULT true, last_seen_at TIMESTAMPTZ, created_by FK→users, created_at | Schema for device auth
2026-04-19 | New API routes: POST /api/kiosk/register (validate token, return branch config), GET /api/admin/kiosk-tokens?branch_id= (list masked), POST /api/admin/kiosk-tokens (generate), DELETE /api/admin/kiosk-tokens/:id (revoke) | Required for kiosk device auth flow
2026-04-19 | WhatsApp customer messages (booking confirmation, queue number): Fonnte is the chosen provider for Phase 2 — no implementation yet; deferred until after mockup approval | Fonnte discussed as the practical WhatsApp API option for Indonesia; Web Speech API remains P1 for barber-side notifications; Fonnte is customer-facing only (confirmations + queue number delivery via WA)
2026-04-19 | WhatsApp provider abstraction: backend notifications.js service exposes sendWhatsApp({ to, templateKey, params }) — routes to Fonnte or WA Business API based on whatsapp_settings.provider; switching providers = change one config row, zero code changes | Fonnte is temporary; abstraction prevents rewriting call sites when migrating to official WA API
2026-04-19 | whatsapp_settings table added (global singleton) — provider ENUM(fonnte, whatsapp_business_api), fonnte_api_key, fonnte_sender, wa_phone_number_id, wa_access_token (future), 3 message templates (booking_confirmation, queue_number, receipt), is_enabled BOOLEAN | Schema required to support configurable provider + templates from admin UI
2026-04-19 | branches.whatsapp_enabled BOOLEAN DEFAULT true added — per-branch opt-out; managed in Branches → Edit → Operations tab | Global switch enables WA for all branches; individual branches can override off
2026-04-19 | Message templates stored in DB (not hardcoded) with {variable} syntax — same templates used by both Fonnte and WA Business API; WA Business API will also require pre-approved Meta template name field (added when switching) | Template copy should not require a code deploy to change; provider-agnostic copy is a migration requirement
2026-04-19 | Settings.jsx: WhatsApp tab added — master on/off toggle, provider selector (Fonnte active / WA Business API greyed Coming Soon), Fonnte credentials, WA Business API credentials section (greyed), template editor with variable chips, test send, per-branch note | Mockup for admin WhatsApp config
2026-04-19 | QueueNumber.jsx: phone prop added; WhatsApp send button shown only when phone was provided — idle/sending/sent states; green WhatsApp styling; sends queue_number template | Customer-facing WA send at queue screen
2026-04-19 | WhatsApp templates expanded to 6 total — 4 customer (booking_confirmation, queue_number, receipt, late_customer_reminder) + 2 staff (barber_new_booking, barber_escalation) | Staff WA needed: barbers get notified on new booking and re-notified on escalation
2026-04-19 | Barber escalation flow: auto WA to barber on new booking; if barber hasn't started by slot_time + threshold → late_customer_reminder to customer (once) + barber_escalation to barber (recurring every N min); stops on booking_started SSE or admin cancels | Core business requirement — no-show handling without front desk
2026-04-19 | Escalation tracking columns added to bookings: escalation_count, escalation_stopped_at, escalation_stopped_by (FK→users, null if auto), escalation_stop_reason ENUM(barber_started, admin_cancelled, max_reached, no_show) | Needed to track state and surface escalation status in admin UI
2026-04-19 | Per-branch escalation settings added to branches table: late_customer_threshold_minutes (DEFAULT 10), barber_escalation_interval_minutes (DEFAULT 3), barber_escalation_max_count (DEFAULT 5) | Per-branch because busy branches may need tighter intervals than quiet ones
2026-04-19 | New API endpoint: PATCH /api/bookings/:id/stop-escalation — admin halts recurring WA without changing booking status | Admin can silence WA escalation while keeping booking alive (e.g. barber is on their way but phone is off)
2026-04-19 | BranchDetail.jsx: escalating bookings show amber left border + "WA Escalating" badge in status column; ActionMenu gains "Stop WA Escalation" item (only when confirmed + escalating); no-show and cancel both auto-stop escalation | Admin needs visibility of which bookings are actively sending escalation messages
2026-04-19 | No manual WA send button on QueueNumber — booking confirmation auto-sends if phone was provided; queue_number template removed entirely; phone prop removed from QueueNumber component | Customer should not need to trigger WA manually; confirmation is sufficient; only customers who provide a number get the message
2026-04-20 | No per-branch WhatsApp enable/disable toggle — WhatsApp is global on/off only; branches.whatsapp_enabled column removed from schema; per-branch toggle note removed from Settings WhatsApp tab | Simpler; no need for branch-level override of a global config switch
2026-04-20 | Fonnte API shape confirmed: POST https://api.fonnte.com/send · Header: Authorization: <token> (no Bearer prefix) · Body: multipart/form-data with fields target (phone number), message (text), countryCode (optional, "62" for Indonesia) · Token from whatsapp_settings.fonnte_api_key · Settings mockup test send is simulated only — real call is backend-only in notifications.js | Confirmed from Fonnte docs; Settings test button will call POST /api/settings/whatsapp/test which proxies to Fonnte server-side
2026-04-20 | Xendit payment failure UX: kiosk shows full-screen error with three options — Retry (re-triggers same terminal session), Try Other Method (switches QRIS↔Card), Contact Staff (prominent CTA + emits SSE alert to admin); booking stays in pending_payment throughout — never auto-cancelled on payment failure | Ensures customer always has a resolution path without needing front desk; staff can take over via Manual Trigger Payment in BranchDetail
2026-04-20 | Feedback tags are admin-configurable — managed from Kiosk Config page (not hardcoded in code); tags have context field: good (4–5 stars), bad (1–2 stars), neutral (3 stars); default tags seeded on first deploy; GET /api/feedback-tags included in kiosk boot response; POST/PATCH /api/feedback-tags for admin CRUD | Tags need to reflect Bercut's brand voice and may change over time; hardcoding prevents admin control without a code deploy
2026-04-20 | No-show and cancel are admin-only actions — barbers have no no-show or cancel button; Live Queue Management (LiveMonitor) is the canonical place for all queue interventions | Prevents barbers from unilaterally removing customers from queue; admin retains control and logs reason
2026-04-20 | BarberPanel: "Tdk Hadir" no-show button replaced with "⚠ Belum Datang" (Client Not Arrived) alert — sends WA to branch backoffice_alert_phone and SSE client_not_arrived event to LiveMonitor; idempotent (toggle shows ✓ after send); barber cannot take further action | Barber alerts admin, admin resolves; reduces risk of accidental no-show marks
2026-04-20 | BarberPanel: Start button is active only for the topmost confirmed booking in the barber's queue — prevents barbers from accidentally starting a different client out of order | UI constraint; backend enforces same rule (can only Start the earliest confirmed booking for the barber)
2026-04-20 | backoffice_alert_phone VARCHAR(25) E.164 added to branches table — WA number that receives barber "client not arrived" alerts; configurable per branch in Branches → Operations tab | Different branches may have different ops contacts; must be per-branch
2026-04-20 | template_client_not_arrived added to whatsapp_settings — ops WA template for client not arrived alert; vars: {barber_name}, {customer_name}, {booking_number}, {branch}, {queue_position} | New WA message type requires its own configurable template
2026-04-20 | bookings.client_not_arrived_at TIMESTAMPTZ added — set when barber taps Belum Datang; cleared if booking is subsequently started normally; used to surface alert badges in Live Queue Management | Persistent flag needed for SSE reconnect (LiveMonitor must know on mount which bookings already have active alerts)
2026-04-20 | LiveMonitor renamed to Live Queue Management — redesigned from read-only CCTV monitor to full admin operations screen; P1 admin actions per booking: Cancel (with required reason), No-show, Move Up / Move Down in queue; alert badges for client_not_arrived events; replaces BranchDetail as canonical place for queue intervention | Admin needs real-time control over queue; read-only monitor is insufficient without front desk staff
2026-04-20 | Any Available barber assignment: fewest any-available bookings today (not fewest total bookings), tiebreak by sort_order — resets midnight per branch; bookings.source ENUM(walk_in, any_available, online) added | Fairness rotation only across any-available pool; direct bookings excluded so popular barbers are not penalised; no persistent pointer needed, count derived at query time
2026-04-20 | Live Queue Management reorder replaced with Start button — Move Up / Move Down removed; admin can Start any confirmed booking (force-start, same endpoint as barber Mulai); simpler and sufficient | Drag-to-reorder is complex; a Start button achieves the same result — admin picks who goes next by starting them
2026-04-20 | POST /api/bookings/:id/client-not-arrived added — sets client_not_arrived_at, sends WA to backoffice_alert_phone, emits SSE client_not_arrived event; idempotent | Required for barber Belum Datang alert flow
2026-04-20 | Late fee grace period: 5 minutes binary threshold — arrive ≤ 5 min late = zero fee; arrive 6+ min late = full late_minutes × late_deduction_per_minute (grace is NOT subtracted from count; 9:06 arrival = 6 min charged, not 1 min) | payroll_settings.late_grace_period_minutes SMALLINT DEFAULT 5 added to schema
